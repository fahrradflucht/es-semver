{
  "name": "semver",
  "version": "5.6.0",
  "description": "The semantic version parser used by npm.",
  "module": "./src/index.js",
  "main": "./lib/index.js",
  "scripts": {
    "test": "tap test/*.js --cov -J",
    "posttest": "standard bin/semver *.js src/*.js"
  },
  "devDependencies": {
    "rollup": "^0.68.0",
    "standard": "^12.0.1",
    "tap": "^12.0.1"
  },
  "license": "ISC",
  "repository": "https://github.com/npm/node-semver",
  "bin": {
    "semver": "./bin/semver"
  },
  "files": [
    "bin",
    "range.bnf",
    "lib",
    "src"
  ]
}
